{
  "name": "rename-preview",
  "displayName": "rename-preview",
  "description": "",
  "version": "1.0.0",
  "repository": "https://github.com/SaidTorres3/live-refactor-vsix",
  "engines": {
    "vscode": "^1.67.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:rename-preview.liveRefactor"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "rename-preview.liveRefactor",
        "title": "Live Refactor"
      }
    ]
  },
  "scripts": {
    "test": "echo \"No tests configured\""
  },
  "devDependencies": {
    "@types/vscode": "^1.67.0"
  }
}
